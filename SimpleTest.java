import static org.junit.Assert.assertEquals;
import org.junit.Test;

public class SimpleTest {
    @Test
    public void testCalculate() {
        //assert statements
        assertEquals("Calculator calculate complete arithmetical expression 4+5",(9));
        assertEquals("Calculator supports the priority of calculation operations 10+5*4+3", (33));
    
        
        
        }
    
}
